# boards/sqscm/

Reserved for the public SQSCM board artifacts (factory health across
Safety, Quality, Service, Cost, Maintainability — First Catch Yield is the
primary Quality gauge, trended by release, gate, standard, defect class,
and model auditor).

Empty until the SQSCM board generator produces its first public artifact.
What appears here is governed by `../../PROOF_BOUNDARY.md`.
